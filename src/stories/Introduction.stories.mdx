import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Example/Introduction" />

# Welcome to Storybook
<br />

## Note!
** We use styled components and If you want to keep all the styling you've applied to a component but just switch out what's being ultimately rendered (be it a different HTML tag or a different custom component), you can use the "as" prop to do this at runtime. see https://styled-components.com/docs/api#as-polymorphic-prop **
